Before starting any coding task, ask whether I want a plan file for
this session — unless I've already stated it explicitly in my request
(e.g. "no plan file" or "make a plan file first").

If I want one: create `plans/yyyy-mm-dd-<short-task-name>-plan.md` containing goal,
files to be touched, approach, and open questions. Do not write code
until this file exists and I've confirmed it.

If I don't want one: proceed directly to code.

If a plan file already exists for the current task (matching by
task name or the most recently active one), update that same
plan file rather than creating a new one. If it's ambiguous which
plan file applies, ask which one.
